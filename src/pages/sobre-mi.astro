---
import Layout from "../layouts/Layout.astro";
import Footer from "@/components/footer/Footer.astro";
import Header from "@/components/header/Header.astro";
import traductions from "@/locales/es/traductions.json";
---

<Layout>
  <Header t={traductions} />
  <main id="about" class="min-h-screen flex items-center">
    <div
      class="max-w-7xl mx-auto px-6 grid grid-cols-1 md:grid-cols-2 gap-12 items-center"
    >
      <!-- Image -->
      <div class="w-full">
        <img
          src="/ruta/a/tu-imagen.jpg"
          alt="Foto sobre mí"
          class="rounded-2xl shadow-lg object-cover w-full h-auto max-h-[500px]"
        />
      </div>

      <!-- Text -->
      <div class="flex flex-col space-y-6">
        <h2 class="text-3xl font-semibold">Sobre mí</h2>
        <p class="text-muted leading-relaxed">
          ¡Hola! Soy <strong class="text-foreground">[Tu Nombre]</strong>, un
          apasionado <span class="italic"
            >[tu especialidad: diseñador, desarrollador, marketer...]</span
          > freelance con más de [X años] de experiencia ayudando a marcas, startups
          y emprendedores a destacar en el mundo digital.
        </p>
        <p class="text-muted leading-relaxed">
          Me especializo en <strong
            >[nombra tus habilidades clave: diseño web, desarrollo frontend,
            branding, etc.]</strong
          >. Me enfoco en ofrecer soluciones creativas, funcionales y hechas a
          medida para cada cliente. Mi objetivo es que cada proyecto no solo se
          vea bien, sino que también cumpla objetivos reales.
        </p>
        <p class="text-muted leading-relaxed">
          Cuando no estoy trabajando en nuevos proyectos, probablemente esté
          aprendiendo nuevas herramientas, explorando diseño UX o tomando café
          ☕.
        </p>

        <a href={`/#${traductions.sections.contact}`} class="cta-primary w-fit mt-4"
          >Hablemos</a
        >
      </div>
    </div>
  </main>

  <Footer t={traductions} />
  <script type="module">
    import { toViewport } from "/animations.js";

    const widthScreen = window.innerWidth;

    if (widthScreen >= 1024) {
      const about = document.getElementById("about");
      toViewport(about);
    }
  </script>
</Layout>
